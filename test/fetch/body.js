'use strict'

const tap = require('tap')
const {
  cancelBody,
  extractBody,
  safelyExtractBody,
  cloneBody,
  mixinBody
} = require('../../lib/fetch/body')

tap.todo('cancelBody')
tap.todo('extractBody')
tap.todo('safelyExtractBody')
tap.todo('cloneBody')
tap.todo('mixinBody')
